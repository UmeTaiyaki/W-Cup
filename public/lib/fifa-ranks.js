// FIFA/Coca-Cola 男子世界ランキング（グループ順位タイブレーカー⑧用 / 純データ・ESM）。
//
// 用途: computeStandings の opts.fifaRank に { CODE: 順位番号 } を渡すと、
//   タイブレーカー⑦フェアプレーポイントでも並ばなかった場合に「順位番号が小さい
//   ほど上位」で最終決着する（FIFA 2026 規定の最終基準。抽選は廃止された）。
//
// 重要: ⑧が発動するのは「全試合勝点・当該チーム間成績・全試合得失差・総得点・
//   フェアプレーポイントまで完全一致」した極めて稀なケースのみ。値が欠けている
//   チーム(undefined)は比較がスキップされ登録順フォールバックとなるため、未記入でも
//   既存挙動を壊さない（graceful degradation）。
//
// データ方針: 数値は「検証済みのFIFA公式ランキングのスナップショット」のみを入れる。
//   未検証・不明は記入しない（捏造しない）。最新ランキング確定後に追記すること。
//   スナップショット日付: （未投入 — 検証済みデータ確定後に記載）
export const FIFA_RANKS = Object.freeze({
	// CODE: rank,  例) "ARG": 1,
});
