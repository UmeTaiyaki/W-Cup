# ビルドなしで .jsx/.js を直接読み込む構成のため、古いキャッシュが残ると
# 更新が反映されない。ソース類は常に ETag で再検証させ、デプロイを即反映する。
/*
  Cache-Control: no-cache

# 管理画面は検索インデックスに載せない（meta robots に加えヘッダでも明示）。
/admin/*
  X-Robots-Tag: noindex, nofollow
